package database

import (
	"testing"
)

func Test_New(t *testing.T) {
	// db, err := New("postgres://127.0.0.1/psychologist?sslmode=disable&user=postgres&password=postgres", 0)
	// if err != nil {
	// 	t.Fatal(err)
	// }
	// defer db.SQL.Close()
}
